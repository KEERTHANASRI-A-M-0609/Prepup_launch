// Vertex seed data — intentionally empty.
// Everything is created by the user through the product.
import type { Application, DSATopic, FailureEntry, Project } from '../types'

export const applications: Application[] = []
export const failures: FailureEntry[] = []
export const dsaTopics: DSATopic[] = []
export const projects: Project[] = []
