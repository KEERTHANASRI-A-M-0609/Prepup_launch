{
  "name": "vertex-placement-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
